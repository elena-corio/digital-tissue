openapi: 3.0.0
info:
  title: Digital Tissue Metrics API
  version: 1.0.0
paths:
  /metrics:
    get:
      summary: Get calculated metrics
      responses:
        '200':
          description: Metrics calculated from the model
          content:
            application/json:
              schema:
                type: object
                properties:
                  metricA:
                    type: number
                  metricB:
                    type: number
